<!--components/song-item-search/index.wxml-->
<view class="item" bindtap="handlePlayClick" data-id="{{songsList.id}}">
<!-- {{item.id}} -->
  <view class="index">{{index+1}}</view>
  <view class="info">

      <text class="name">{{songsList.name}}</text>
    <view class="source">
      <text class="desc">{{songsList.artists[0].name}}</text>
      <text class="alias" wx:if="{{songsList.alias[0]}}" decode="true">&nbsp;· {{songsList.alias[0]}}</text>
    </view>
  </view>
</view>

