<!--components/song-item-v1/index.wxml-->
<view class="item" bindtap="handlePlayClick" data-id="{{item.id}}">
  <image src="{{item.al.picUrl}}" mode="" class="image"/>
  <view class="content">
    <view class="name">{{item.name}}</view>
    <view class="source">{{item.ar[0].name}} · {{item.al.name}}</view>
  </view>
  <view class="arrow">
    <image src="/assets/images/player/rightArrow.png" mode="" class="icon"/>
  </view>
</view>
